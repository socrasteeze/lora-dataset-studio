const n="Comfyui_TTP_Toolset",o="https://github.com/TTPlanetPig/Comfyui_TTP_Toolset";function l(t){const e=t&&t.comfyui||{};if(!e.reachable)return{state:"unknown",text:"Start ComfyUI and this page will tell you whether the tiling pack is there."};if(e.seedvr2_tiling_ready)return{state:"ready",text:"Ready — large upscales are cut into tiles automatically, so they no longer have to fit on the card in one piece."};const i=e.seedvr2_tiling_nodes_missing;if(!Array.isArray(i)||!i.length)return{state:"unknown",text:"ComfyUI is reachable, but its node list could not be checked. Re-check in a moment to confirm whether tiling is available."};const r=i.filter(a=>!a.startsWith("TTP_"));return r.length?{state:"restart",text:`Update ComfyUI and restart it: its standard image/mask nodes (${r.join(", ")}) are missing. They are needed to preserve overlap at tile boundaries; installing the TTP pack alone does not add them.`}:i.length&&i.length<2?{state:"restart",text:`The ${n} pack is there but does not expose ${i.join(", ")} — update it in ComfyUI-Manager and restart ComfyUI.`}:{state:"absent",text:`Optional. Without it, upscales still run — they are just limited to what this GPU can hold in one pass. Install ${n} in ComfyUI-Manager and restart ComfyUI to lift that limit.`}}function u(t,e,i){const r=Number(e),a=Number(i);if(!(r>0))return null;if(t==="never")return`Nothing is tiled: your ${r} px target runs full-frame.`;if(t==="always")return`Your ${r} px target is tiled whenever it comes out bigger than one tile.`;if(!(a>0))return null;if(r>a)return`Your ${r} px target is above the ${a} px crossover, so it is tiled.`;const s=r===a?`exactly at the ${a} px crossover, so it runs full-frame — the crossover has to be passed, not just reached`:`below the ${a} px crossover, so it runs full-frame`;return`Your ${r} px target is ${s}. To tile it: raise the target above ${a} px, lower “Start tiling above”, or pick “Always tile large frames”.`}function h(t){const e=(t&&t.comfyui||{}).seedvr2_ceiling_mp;return typeof e!="number"||!(e>0)?null:(t&&t.comfyui||{}).seedvr2_tiling_ready?`This GPU is good for roughly ${e} MP in a single pass; anything larger is tiled.`:`This GPU is good for roughly ${e} MP in a single pass. Past that an upscale may run out of memory — that is what the tiling pack below is for.`}export{n as T,o as a,h as c,u as l,l as t};
