"""Shared model catalogue; retained Video import path."""
from lds_sdk.h3_targets import (
    PROFILE_KEYS as PROFILE_KEYS,
    clip_seconds as clip_seconds,
    frame_choices as frame_choices,
    get as get,
    is_legal_frames as is_legal_frames,
    snap_frames as snap_frames,
    validate_resolution as validate_resolution,
    wants_audio as wants_audio,
)
