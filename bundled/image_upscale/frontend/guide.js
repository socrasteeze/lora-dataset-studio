export const GUIDE = {
  "chapters": [],
  "sections": [
    {
      "chapter": "settings-reference",
      "anchor": "improve-finishing",
      "markdown": "## Improve finishing\n\nThree pixel operations **Klein Improve** runs on its finished image, after ComfyUI is done with it — no model, no GPU, no node pack. They apply to the ✨ improve of a dataset image, of a Bank image and of a Canvas board picture alike, and two of them (sharpen, grain) are also available per run in the Test Studio's Engine section. All three ship **off**: an untouched install keeps the exact bytes ComfyUI wrote, and the pass does not even re-encode. The order is fixed — colours, then sharpen, then grain — because colour matching estimates a transform from image statistics (it must see the render before sharpening adds halos) and grain must come last (sharpening amplifies exactly the frequencies grain adds).\n\n\n- **Put the source's colours back** → `improve.colour_match`. Range `0`–`1`, default **`0`** (off). How much of the source's grade to restore after the pass: a Klein pass at full denoise keeps the content and loses the colours (skin warms or cools), and a dataset ends up holding two colour worlds. The reference is the image as it was **before** the pass. `0.8` is the value of the reference workflow this was ported from; `1` forbids the pass any colour change. **Applies to passes Klein ran only**: a SeedVR2 pass already grades its result back onto the source inside the node, and it is skipped automatically there — the engine is read off the image's own record, never off the default above.\n- **Sharpen** → `improve.sharpen`. Range `0`–`1.5`, default **`0`** (off). Local contrast at a 1 px radius — the finest octave, the one diffusion leaves empty — not a global sharpness slider. `0.55` is the reference value; past about `1` the halo starts reading as an outline.\n- **Film grain** → `improve.grain`. Range `0`–`0.05`, default **`0`** (off). Standard deviation of the added noise in 0–1 units: `0.01` (the reference value) is about ±2.5 levels of an 8-bit image, read as texture, never as noise. It is what stops a render looking plastic. The scale is deliberately tiny — values that look sensible by analogy with other sliders (`0.1`, `0.3`) are heavy noise.\n- **How coloured that grain is** → `improve.grain_saturation`. Range `0`–`1`, default **`0.2`**. `0` puts identical noise on the three channels (what film does), `1` makes each channel independent (reads as sensor noise). Shared by the improve pass and by the Test Studio's per-run grain, so it stays editable even while the improve grain is off.\n\n\n"
    },
    {
      "chapter": "using-the-app",
      "anchor": "upscale-a-picture-straight-from-the-board",
      "markdown": "## Upscale a picture straight from the board\n\nThis combination requires **Canvas** and **Improve & upscale**. Improve & upscale also works on Banks, datasets and Gallery without Canvas.\n\nClick a pinned picture (🔍, or the picture itself) and the full-screen view now\ncarries **✨ Upscale & improve** next to **⬇ Download** — the same pass, and the\nsame choice of engine, as the one in the dataset lightbox.\n\nThe same button is on the **checkpoint and run galleries** — open a picture from\na pill's 🖼 gallery, or from a run card, and it is there too. That is where an\nimprovement is delivered, so it is where the gesture costs the fewest clicks:\nyou are already comparing a checkpoint's renders when you decide one of them\ndeserves a bigger pass. Both surfaces are the same action on the same picture:\n\n- **✨ Improve via Klein** re-renders detail and texture. Sharper, but skin and\n  colour can shift. Pressing it opens a small settings window that quotes the\n  exact instruction it is about to send — editable in place, or switched off —\n  with the Klein model, LoRA preset and output size, and a **✨ Generate**\n  button. Stay, and the finished picture appears right in that window.\n- The separate **SeedVR2** plug-in can add faithful restoration to the same\n  galleries. It is optional; Klein Improve works on its own.\n\n**Where the result goes.** The picture you started from is never touched. The\nimprovement arrives as its **own image in that checkpoint's gallery**, right next\nto the original — open the gallery from the checkpoint pill (🖼) and you can\ncompare the two, download either, or pin the improved one onto the board beside\nits source. A Klein pass shows its result **in the ✨ window itself** if you stay\non it; close the window early (or run SeedVR2, which has no window) and nothing\nmoves on its own, which is why the confirmation says where to look. The pass\ntakes minutes, and a gallery already open does not refresh by itself: close it\nand open it again to find the new picture waiting at the top.\n\nTwo things it deliberately will not do. An **improvement cannot be improved\nagain** — running two passes over the same pixels is how a face turns to\nplastic — and the **lane's reference face** has no ✨ at all, because it is a\nphoto you supplied, not something the app generated. If a pass fails, press ✨\nagain: that is the retry.\n\n**It stays out of the Test Studio.** These upscales are not sweep cells, so they\nnever appear in the Test Studio grid, never count as a run in progress, and never\nenter the 👍/👎 ranking of a checkpoint — a rating you give an *upscale* would\notherwise be read as a vote for the checkpoint that did not produce it.\n"
    },
    {
      "chapter": "settings-reference",
      "anchor": "klein-improvement-settings",
      "markdown": "## Klein improvement settings\n\nOpen **Plugins → Klein Improve → Settings** for these preferences. The default image improvement engine (`improve.engine`) is shared across installed improvement plug-ins and can be changed from either product’s settings. SeedVR2 restoration is a separate optional plug-in. The core Klein generation and reference-edit controls remain separate.\n\n`identity_prompts.klein_improve` and its toggle stay **global** on purpose — \"add texture and detail\" means the same thing for a person, a dog or a car. **It does not mean the same thing for a drawing**, and that is a known rough edge: the shipped text asks for photographic detail, so on an **Anime** dataset the improve pass works against the very art style every other prompt in the app protects. Until that is settled, edit the box (or turn it off) for drawn datasets — see *Troubleshooting → \"Upscale & improve\" makes my anime look realistic*.\n\n**Output size (MP)** is also editable from the improve note itself — the panel under the ✨ button — same key, same 0.5–8 bounds, app-wide like the instruction.\n\n**LoRA preset on ✨ Upscale & improve** → `klein.improve_lora_preset` (a preset **name**, default **blank = none**). Which of your **generation LoRA presets** (the named combinations defined on this card) every Klein improve chains after the consistency LoRA. Picked from the improve note itself — the settings window the ✨ button opens (inline on the bulk toolbar), next to the instruction editor — and **app-wide like the instruction**: the single ✨, the 🔄 re-run and the whole batch all follow it, in every dataset. Fail-closed like the rest of the preset chain: a renamed or deleted preset quietly runs as **None**, never a blocked pass. **SeedVR2 is unaffected** — a restoration chains no LoRA. The improved image records the LoRAs that actually ran in its details, so a render never claims a preset it did not use. Once a preset is picked there, the window also lists **its LoRAs with a strength slider each** — the preset's own values, app-wide, so lowering one applies wherever that preset runs; building the preset itself stays on this card.\n\n**Edit the improve instruction from the ✨ button itself.** `identity_prompts.klein_improve` is the improvement instruction you never have to come here for: the improve note carries **✎ Edit this instruction here**, which opens the same box and the same on/off tick in place. In the dataset and generated-image lightboxes that note is the settings screen of the **window the ✨ button opens** (press ✨, read, edit, then **Generate**); on the bulk toolbar it stays inline, because a batch shows its instruction before launching a lot. It edits **this setting** — there is no per-image and no per-run copy, so what you write there is what this card shows and what every later improve sends, in every dataset. The panel says that in as many words before you type, because a control sitting inside one dataset's screen otherwise reads as belonging to that dataset. The rest of the contract is identical to the box here: the field is pre-filled with the text actually in force, text matching the built-in default is stored as blank, **Reset to default** shows up only once you really have an override, and edits save as you type (the last keystroke is saved even if you close the lightbox on it). What is *not* duplicated there, on purpose: the per-subject identity locks, the other prompt parts and the other generation settings.\n\n- **Klein upscale & improve prompt** → `identity_prompts.klein_improve`, with an on/off toggle `identity_prompts.klein_improve_enabled` (default **on**). The fixed instruction the manual **Klein upscale & improve** action sends to add texture and detail. The shipped text is `add detailed texture, add sharp details, add candid shot, add soft focus effect` — read it before you blame the model: those four clauses describe a **photograph**, and they are applied to every dataset, drawn ones included. **Turn the toggle off** to run that action with **no prompt at all** — a pure upscale with no added styling. Both the text in force and these two levers are now quoted **and editable** from the ✨ **Upscale & improve** button itself (the settings window it opens in the lightboxes, the inline note on the bulk toolbar) — see *Edit the improve instruction from the ✨ button itself* above — so you no longer have to know this setting exists to find it, nor leave your images to fix it.\n- **Upscale & improve — strength** → `klein.improve_megapixels`, `klein.improve_base_lora_strength`, `klein.improve_consistency_strength`, `klein.improve_steps`. The output resolution, and how much that pass is allowed to change the image. Until these were exposed the whole profile was hardcoded — the enhancement LoRA disabled, so the *enhancement* LoRA baked into the workflow never applied at all, and the size was fixed at 2 MP whatever the source was worth. Defaults are those same historical values (**2 MP / 0 / 1 / 4 steps**), so leaving them alone keeps today's result exactly. These are read at each run, so to try a new value on an image you already improved, use the **🔄✨** button on that tile: it re-runs the pass on the same source image with the settings as they are now, and replaces the result in place. (The ordinary 🔄 stays hidden there — it would restart from the dataset's reference photo and make an unrelated image.)\n  - **Output size (MP)** (0.5–8, default **2**) — the source is rescaled to this pixel budget before sampling, so it *is* the result's resolution. This is the knob that makes \"Upscale\" actually upscale.\n  - **Enhancement LoRA** (0–2, default **0**) — the workflow's own detail LoRA. At 0 it does nothing; try **0.5–0.8**. It needs its weights file (`klein/realistic.safetensors`): a positive strength refuses the pass when that file is missing. **Setup ▸ Downloads & repair offers it** with the other Klein assets (from [dx8152/Flux2-Klein-9B-Enhanced-Details](https://huggingface.co/dx8152/Flux2-Klein-9B-Enhanced-Details), Apache-2.0) — install it before requesting a positive enhancement strength.\n  - **Consistency LoRA** (0–1.5, default **1**) — anchors the **composition and background**, not identity. Deliberately high here: an improve pass should add detail *without* redrawing the shot, so the resistance to editing that makes 0.8–1.0 a poor choice for restaging is exactly what you want. (Shipped briefly as `improve_character_lora_strength`, a misnomer; a value saved under that name is still honoured.)\n  - **Steps** (1–50, default **4**) — more steps is slower and usually cleaner.\n  - Out-of-range or malformed values are **clamped**, never rejected: a bad config weakens the pass instead of failing your click.\n  - **A strength you raised is never silently dropped.** If a LoRA's weights file is missing while its strength is above 0, the pass reports the missing asset (which is what triggers its download) instead of running without it and leaving you to wonder why nothing changed. At strength 0 it stays a quiet skip — nothing is lost by not loading a LoRA you did not ask for.\n"
    }
  ]
}
