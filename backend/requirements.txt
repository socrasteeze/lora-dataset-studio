Flask==3.1.3
Flask-SQLAlchemy==3.1.1
Flask-WTF==1.3.0
SQLAlchemy==2.0.52
Werkzeug==3.1.8
requests==2.34.2
python-dotenv==1.2.3
Pillow==12.3.0
psutil>=5.9.8
huggingface-hub>=0.24     # Publish dataset to the HF Hub (pure-python, light): HfApi + upload_folder
PyYAML>=6.0               # Parse ComfyUI's extra_model_paths.yaml (was only transitive via huggingface-hub); guarded import degrades gracefully if absent
Send2Trash>=1.8           # Bank "Delete rejected from disk" -> OS trash instead of a hard delete (pure-python, tiny); guarded import degrades to os.remove if absent
# Test dependencies are NOT here: this file is what every end user installs
# (start.bat), and it shipped a test runner nobody runs. They live in
# requirements-dev.txt, the common CI/release test baseline. Runtime bridge tests
# add the separate CPU-only requirements-torch-tests.txt overlay -- see its header.
